/**
 * Simple controllers implementation used independent of any web framework. Those are not implemented as servlets but
 * rather CDI beans injected into request dispatching servlet. Those could be implemented as servlets, but it would
 * cause unnecessary redundant code.
 */
package pl.edu.pg.eti.kask.wingspan.musician.controller.simple;
